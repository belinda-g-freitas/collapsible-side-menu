class SideMenuItemAnimationData {
  final double mainScale;
  final Duration duration;

  const SideMenuItemAnimationData({this.mainScale = 0.95, this.duration = const Duration(milliseconds: 500)});
}
